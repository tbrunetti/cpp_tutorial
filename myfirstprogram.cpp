#include <iostream>  //header file library
using namespace std; //use names for objects and variables from the standard library


/* function called main; note all cpp code ends with semi-colon (similar to Java and ANSII C) */
int main() {
    cout << "Hello World!"; // cout is the oject and when paired with the insertion operator (<<) prints object to standard out 
    // std::cout << "Hello World!"; is the same as the above line without having to list the "using namespace std;"
    return 0;
}

